// it requires the sum.js/sustraction.js/multiplication.js/division.js files

var sum = require('./calculator').sum
var multiplication = require('./calculator').multiplication

console.log('the sum is' + sum(3, 4))

// the .sum represent the object sum

// then in terminal node app_sum.js
